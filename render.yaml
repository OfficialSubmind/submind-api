services:
  - type: web
    name: submind-oneclick
    env: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    envVars:
      - key: WORKER_INTERVAL
        value: "20"
